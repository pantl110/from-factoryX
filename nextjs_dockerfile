# 베이스 이미지: Node.js LTS (예: 20.x)
FROM node:20-alpine

# package.json과 package-lock.json 복사
COPY ./frontend/package*.json ./

# 의존성 설치
RUN npm install

# 나머지 소스 코드 복사
COPY ./frontend /app

# 작업 디렉토리 설정
WORKDIR /app

# 빌드
RUN npm run build

# Next.js 앱 실행
CMD ["npm", "start"]
